{
  "name": "FlowSpeak - Post Intent to API (example)",
  "nodes": [
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "webhook/flowspeak",
        "responseMode": "onReceived",
        "options": {}
      },
      "name": "Webhook",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 1,
      "position": [250, 300]
    },
    {
      "parameters": {
        "authentication": "headerAuth",
        "url": "={{$env.HOME_API_URL || `http://localhost:5000`}}/api/action/process",
        "options": {},
        "headerParametersJson": "[{\"name\":\"Content-Type\",\"value\":\"application/json\"}]",
        "bodyParametersJson": "={{$json}}"
      },
      "name": "Post to FlowSpeak API",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 1,
      "position": [600, 300]
    }
  ],
  "connections": {
    "Webhook": {
      "main": [
        [
          {
            "node": "Post to FlowSpeak API",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  }
}
